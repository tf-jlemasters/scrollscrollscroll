<p style="color: green"><%= notice %></p>

<h1>Comments</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render comment %>
    <p>
      <%= link_to "Show this comment", comment %>
    </p>
  <% end %>
</div>
  <%== pagy_next_link(@pagy, text: 'Next page', link_extra: 'id="next_link"') %>
<%= link_to "New comment", new_comment_path %>
